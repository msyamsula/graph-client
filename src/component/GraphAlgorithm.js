let GraphAlgorithm = (props) => {
    let style = {...props.style, ...{
        textAlign: "center"
    }}
    return (
        <div style={style}>this is algorithm</div>
    )
}

export default GraphAlgorithm